package br.ada.caixa.service;

import br.ada.caixa.repository.ContaRepository;

public class ContaCorrentePJService extends ContaService {
    public ContaCorrentePJService(ContaRepository contaRepository) {
        super(contaRepository);
    }
}
