package br.ada.caixa.service;

import br.ada.caixa.repository.ContaRepository;

public class ContaInvestimentoPFService extends ContaService {
    public ContaInvestimentoPFService(ContaRepository contaRepository) {
        super(contaRepository);
    }
}
