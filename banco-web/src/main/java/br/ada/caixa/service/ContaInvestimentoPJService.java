package br.ada.caixa.service;

import br.ada.caixa.repository.ContaRepository;

public class ContaInvestimentoPJService extends ContaService {
    public ContaInvestimentoPJService(ContaRepository contaRepository) {
        super(contaRepository);
    }
}
