package br.ada.caixa.service;

import br.ada.caixa.repository.ContaRepository;

public class ContaCorrentePFService extends ContaService {
    public ContaCorrentePFService(ContaRepository contaRepository) {
        super(contaRepository);
    }
}
